import {MoviesList} from "../../components/MoviesContainer/MoviesList/MoviesList";

const MoviesPage = () => {
    return (
        <div>
            <MoviesList />
        </div>
    );
};

export {MoviesPage};